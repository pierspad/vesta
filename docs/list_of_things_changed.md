# Modifiche recenti (in preparazione alla release)

## 2026-07-11 — Release automation, benchmark SVG, hardening API/tier

* **Release 100% automatizzata — nuova action `release.yml`**: bump SemVer del `pkgver` (patch/minor/major o versione esplicita) via `workflow_dispatch`, propagazione con `update_project_info.sh`, gate di coerenza versioni + audit i18n, release notes rolling riallineate (o rigenerate dai commit se stantie), commit+tag+push atomico, dispatch esplicito di "Build and Release" sul tag (i tag pushati con `GITHUB_TOKEN` non scatenano `on: push`), e job di **backmerge** `main↔dev`. Rimossi gli script manuali resi obsoleti: `git-release.sh`, `push-aur.sh`, `build-aur.sh`, `PKGBUILD.local`, `vesta-bin/` (restano solo i file usati dalla CI).
* **Benchmark — auto-discovery dei film**: `benchmarks/config.sh` ora scansiona `Test_Subs/FILM/` (`<Titolo>-en.srt` + `<Titolo>-it.srt` + video `<Titolo>*.{mp4,mkv,avi,m4v,webm}`); i film incompleti (es. "8 e mezzo") vengono saltati con warning. Basta copiare i tre file per aggiungere un film.
* **Benchmark — output SVG (renderizzato da GitHub nel markdown)**: `report/plot.py` produce `results/benchmark.svg` (grafico combinato, scala log) **più un grafico per film** in `results/films/<slug>.svg` con speed-up annotato sulle barre; `4_generate_report.sh` genera un `summary.md` dettagliato (tabella per film con cards/min, aggregato con speed-up complessivo per serie — a parità di film —, note metodologiche e grafici embeddati).
* **Fix resume traduzione con "buchi"**: con un output parziale contenente righe mancanti/incoerenti, il run ripartiva ritraducendo *tutto* con numerazione batch oltre il 100%. Ora: prefisso completo → salta i batch fatti; pochi buchi (≤ 2×batch) → direttamente il repair mirato con contesto; molti buchi → ritraduzione in batch con numerazione corretta. (Applicato sia al percorso tiered sia a quello legacy in `lib/srt-translate`.)
* **Fix failover tier troppo aggressivo**: `is_rate_limit_error` non matcha più il generico "exceeded" (un "context length exceeded" marcava la entry come esaurita e faceva scalare il tier); aggiunti i pattern specifici `limit exceeded`/`resource exhausted`.
* **Hardening pool di traduzione**: `build_pool` ora scarta le entry di provider remoti senza API key (defense-in-depth: il docstring lo prometteva già ma il filtro mancava — una config malformata produceva batch falliti a runtime).
* **Rimosso codice morto API config**: eliminati il comando `set_api_config` e i campi `api_key`/`api_type` da `TranslateState` — la configurazione viaggia per-run nei `tiers` di `TranslateConfig`, mai persistita nel backend.
* **Sicurezza media server locale**: `/media?path=…` serviva qualsiasi file su 127.0.0.1 con CORS aperto (qualunque pagina web o processo locale poteva leggere file arbitrari). Ora ogni richiesta richiede un token di sessione (128 bit, generato all'avvio) noto solo alla webview: nuovo comando `get_media_server_info` (porta+token), 403 senza token; aggiornati SyncTab/FlashcardsTab.
* **Fix migrazione chiavi legacy**: `loadAndValidateApiKeys` non forza più a `google` le chiavi di tipo sconosciuto che non iniziano con `AIza` (producevano 401 criptici) — vengono scartate e l'utente le ri-aggiunge col provider corretto.
* **Harness `.cursorrules` riscritto nelle sezioni release (§14–16), shortcuts (§8.1), select (§5.5)** e ricopiato identico in vesta/textmerger/reaper/root: documenta la pipeline Actions (release semver → build → AUR → backmerge, incluso il bootstrap AUR al primo push), il tab shortcuts con ricerca testuale+"tastariale" e `SearchableSelect` come componente canonico.

## Feedback tester Windows + Modalità Easy/Esperto

* **Fix download FFmpeg (Windows)**: `flashcard_download_ffmpeg` ora verifica davvero l'installazione — dopo l'unpack i binari vengono "issati" alla radice di `ffmpeg_bin` (layout degli archivi variabile fra release di `ffmpeg_sidecar`), e il comando ritorna `Ok` solo se `ffmpeg -version` gira davvero. Errori dettagliati per download/unpack falliti.
* **Fix banner "FFmpeg non trovato" che ricompariva**: `flashcard_check_deps` usa ora la stessa identica logica di risoluzione di chi invoca ffmpeg (PATH → app-data → cartella dell'eseguibile) ed esegue il binario invece di controllare solo l'esistenza del file. Dopo un download riuscito le tab si ri-sincronizzano fra loro con l'evento `vesta-ffmpeg-updated` invece di impostare ottimisticamente lo stato. Supportato ufficialmente ffmpeg copiato accanto all'eseguibile di Vesta (il workaround del tester ora è un percorso di risoluzione).
* **Fix barra trascrizione ferma al 30%**: il callback dei segmenti whisper-rs ora emette il progresso reale mappando la posizione nell'audio sulla finestra 30–90% della barra.
* **Fix banner attaccato al bordo alto** nella tab Trascrizione (ora staccato con margine, come gli altri popup).
* **Traduzione — esito onesto e "Ripeti traduzione"**: un run che termina con 0 sottotitoli tradotti non è più mostrato come successo verde; il messaggio di esito è localizzato lato frontend. Aggiunto il pulsante **Ripeti traduzione** (stessi file e impostazioni) accanto a "Nuova traduzione".
* **Fix freeze del PC durante la generazione del mazzo (Windows)**: tutti i processi ffmpeg/ffprobe (estrazione media, conversioni whisper) partono con `BELOW_NORMAL_PRIORITY_CLASS` + `CREATE_NO_WINDOW` — il sistema resta reattivo anche a piena parallelizzazione e niente flash di finestre console.
* **Modalità Easy/Esperto**: nuovo switch in sidebar sopra il Kill Switch AI (stesso stile, persistito in `vesta-expert-mode`, store `uiModeStore.svelte.ts`). In Easy (default): nome mazzo automatico, export forzato `.apkg`, core CPU = n-1, pannelli Filtri carte/Naming nascosti, parametri avanzati audio/video/snapshot nascosti (restano i toggle e la scelta della traccia audio), niente selettore formato/template nella barra inferiore; in Trascrizione nascosti word-timestamps e lunghezza segmenti, in Traduzione nascosti batch size/overlap/contesto. Esperto = interfaccia completa attuale. Tutto reattivo al cambio di modalità e compatibile con Kill Switch AI on/off.
* **Prompt di refinement riscritto (few-shot, conciso)**: nuova fonte unica `lib/refinementPrompt.ts` (eliminata la duplicazione RefineTab/SettingsTab, ~150 righe): spiega solo le parole/espressioni davvero difficili (max 3 per carta, una riga ciascuna), etimologia solo per termini astrusi, output "—" se non c'è nulla da spiegare, esempi few-shot EN/DE per calibrare il modello. Migrazione automatica dai vecchi prompt di default (i prompt personalizzati sono preservati).
* **i18n — stringhe hardcoded eliminate e checker automatico**: tradotte in tutte le 15 lingue le stringhe fisse segnalate ("New Translation", "Scarica Automaticamente", motivi di blocco trascrizione/traduzione, drop-zone, anteprima riproduzione flashcard, Film/Serie TV/Add Files, requisiti generazione, LLM Engine ecc.). Nuovo `scripts/check-i18n.mjs` (`npm run check:i18n`): parità chiavi fra locali, chiavi `t("...")` usate ma assenti, coerenza dei placeholder `{{var}}`, euristica per testo hardcoded nei template.


## Modularizzazione headless dei Flashcard + pipeline di benchmark

* **Nuovo crate `lib/srt-flashcards`** (libreria GUI-agnostica): estratta tutta la logica flashcard (parsing SRT/ASS/VTT, matching doppi sottotitoli, filtri, contesto, estrazione media via FFmpeg, export TSV e APKG) da `apps/srt-gui/src-tauri/src/commands/flashcards/` in un crate riusabile e **senza alcun accoppiamento a Tauri**, come da regola §9 del `.cursorrules`. L'API pubblica espone `generate()` (callback di progresso generica + `CancellationToken` + `MediaTools` per i percorsi ffmpeg/ffprobe), `preview()`, `load_sub_file_info()`, `list_audio_tracks()`, `check_ffmpeg()` e il pipeline condiviso `build_matched_lines()`.
* **Nuova CLI headless `cli/srt-flashcards-cli`** (binario `srt-flashcards`): front-end a riga di comando completo (clap) che dipende **solo** da `lib/srt-flashcards`. Permette di clonare il repo e compilare/usare i soli flashcard da terminale come tool headless, con sottocomandi `generate`, `info`, `preview`, progresso throttellato su stderr e cancellazione via Ctrl-C. Stesso identico motore della GUI.
* **App Tauri ridotta ad adattatore sottile**: `commands/flashcards/commands.rs` ora si limita a tradurre fra il mondo Tauri (AppHandle, stato gestito, eventi `flashcard-progress`) e il motore puro. Aggiunto uno shim `commands::flashcards::types` e un piccolo modulo `media` di risoluzione ffmpeg/ffprobe condiviso con transcribe/sync, così `main.rs` e i test esistenti continuano a compilare senza modifiche.
* **Deduplicazione**: il blocco `parse → time-shift → match → span → filtri → combine → context`, prima copia-incollato fra `flashcard_preview` e `perform_generation`, è ora un'unica funzione `build_matched_lines()` condivisa (eliminata anche una `clone()` superflua dei sottotitoli nativi).
* **Performance — estrazione media in streaming**: sostituita la vecchia esecuzione "a batch con barriera" (`chunks(batch_size)`, che attendeva il task più lento di ogni batch) con un limitatore a **semaforo + `JoinSet`** che mantiene sempre esattamente `core-1` processi ffmpeg in volo. Nessuna barriera fra i batch → migliore saturazione dei core sul percorso dominante.
* **Pulizia dipendenze**: rimosse da `apps/srt-gui/src-tauri/Cargo.toml` le dipendenze dirette ora inutilizzate (`rusqlite`, `sha1_smol`, `zip`), spostate nel crate libreria.
* **Qualità**: `cargo clippy` pulito su libreria e CLI (digit grouping, `split_once`, `collapsible_if`, slice al posto di `&mut Vec`, ecc.); aggiunto `lib/srt-flashcards/tests/pipeline.rs` (regressione veloce, senza GUI, sui sottotitoli reali di *Detour*).
* **Harness headless di subs2srs** (`benchmarks/subs2srs-headless/`): "iniettore custom" che compila i **veri** worker di subs2srs (`WorkerSubs/Srs/Audio/Snapshot/Video`, `Utils*`) in un eseguibile console, sostituendo solo tre tipi WinForms con stub no-op (`DialogProgress`, `UtilsMsg`, shim `TagLib`). Replica la sequenza di `SubsProcessor.bw_DoWork` **senza alterare la logica di generazione**: subs2srs resta single-thread e sequenziale. Confronto onesto.
* **Pipeline di benchmark numerata** (`benchmarks/`): `1_compile_subs2srs.sh`, `2_compile_vesta.sh`, `3_run_benchmarks.sh`, `4_generate_report.sh` + `config.sh` (media di test + varianti di Vesta confrontabili) + grafico (matplotlib PNG, con fallback SVG senza dipendenze). Vesta gira sul piano max (core-1), subs2srs sul suo comportamento nativo.


* **UI Shell & Sidebar Layout**: Standardized padding of the first card/panel in Svelte tabs (`AlignTab`, `FlashcardsTab`, `SyncTab`, `LogPanel`) to `p-5` (or `px-5`) and unified heading icon markup (nesting SVG icons inside heading tags and updating tag names) to resolve vertical baseline offsets and ensure perfect visual continuity.
* **Keyboard Shortcuts**: Added global `Ctrl + PageUp` and `Ctrl + PageDown` keyboard shortcuts in `App.svelte` for quick tab switching/navigation.
* **Frontend Optimization & Boilerplate Reduction**: Established a central, Svelte 5 Rune-based `snackbarStore.ts` to manage toast notifications, rendering a single `<Snackbar>` globally in `App.svelte` and refactoring all 6 main tabs (`FlashcardsTab`, `SettingsTab`, `SyncTab`, `TranscribeTab`, `TranslateTab`, `AlignTab`) to eliminate duplicate local state variables, timers, and redundant component templates.
* **Consolidation of Shared Utilities**: Standardized helper functions (`getFileName`, `inferLanguageFromPath`, `getFlagForPath`) under `models.ts` and successfully removed identical duplicate function definitions in `AlignTab.svelte`, `FlashcardsTab.svelte`, and `SyncTab.svelte` to unify file parsing logic and decrease LoC.
* **Lifecycle Consolidation & Memory Leak Prevention**: Merged multiple duplicate `onMount` blocks in `SettingsTab.svelte` into a single unified block with clear lifecycle boundaries. Properly resolved a global `"whisper-model-updated"` event listener memory leak in `TranscribeTab.svelte`.
* **Race-Condition Protected Tauri Subscriptions**: Re-engineered all asynchronous Tauri event registrations (`listen` and `onDragDropEvent`) in `TranslateTab`, `TranscribeTab`, `AlignTab`, `SyncTab`, `SettingsTab`, and `FlashcardsTab` using a latch-guard pattern to automatically prevent event listener leaks when components unmount before subscriptions resolve.
* **Passive CPU & IPC Usage Optimization**: Configured Svelte `$effect` triggers in `TranslateTab.svelte` to stop background subtitle preview polling when the tab is inactive, preserving IPC and CPU cycles while the application is in an idle state.
* **Frontend Unification & Boilerplate Reduction**: Unified the `handleTabChange` and `changeTab` functions in `App.svelte` and corrected keydown listener references, eliminating duplicate handlers and centralizing navigation logic.
* **Reusable Component Consolidation**: Integrated the global `<InfoModal>` component into `AlignTab.svelte` and removed the inline manual dialog HTML/CSS structure, resulting in over 30 lines of code reduction (LoC) and guaranteeing complete UI and layout consistency.
* **Context Menu Keyboard Shortcuts Accessibility**:
  * Added visual keyboard shortcut labels (e.g. `H`, `S`, `R`, `C / Ctrl C`, `X / Ctrl X`, `V / Ctrl V`, `A / Ctrl A`) and global shortcut key interception to standard right-click and settings notification context menu items in `AppContextMenu.svelte`.
  * Added single-key keyboard bindings (`U`, `R`, `C`, `X`, `V`, `A`) and visual labels to the code editor context menu in `CodeEditor.svelte`.
  * Added visual keyboard shortcuts (`P`, `G`) and active key interception to the subtitle sync context menu in `SyncTab.svelte`.
  * Added visual keyboard shortcuts (`E`, `S`, `D / Del`) and implemented global window key capturing using `<svelte:window>` in `FlashcardsTab.svelte` when the episode list context menu is active.
  * Appended a context menu keyboard shortcut accessibility rule to `.cursorrules` to govern future context menu development.
* **Aggiornamento delle Regole di Sviluppo (.cursorrules)**:
  * Ricreato e aggiornato il file `.cursorrules` di Vesta integrandolo con le regole e i principi avanzati di Armonia Visiva UI/UX (CSS shimmer skeletons, layout validation, CSS logical properties, empty state guidelines, e ottimizzazioni GPU desktop Tauri).
* **Raffinatezze UI/UX e Allineamenti di Layout**:
  * **Shortcuts Tab**: Impostata la selezione delle categorie predefinita a nessuna categoria selezionata (array vuoto `[]` all'avvio) per mostrare tutte le scorciatoie di default.
  * **Feedback Registrazione**: Rimosso il banner grafico di successo locale e integrato con chiamate alla snackbar globale con durata impostata a 2.5 secondi.
  * **Pulsanti di Reset**: Aggiornato il testo del pulsante e del dialogo di reset da "Ripristina Tutte" a "Ripristina Predefiniti" (utilizzando la localizzazione `settings.resetDefaults`).
  * **Allineamento Spaziale delle Bande**: Modificata l'altezza della banda superiore (`h-[89px]`) e inferiore (`h-[92px]`) di `ShortcutsTab.svelte` per combaciare esattamente con i divisori della sidebar.
  * **Sidebar**: Centrate verticalmente le 5 voci principali di navigazione introducendo uno spacer flessibile superiore, ed incrementato lo spazio tra di esse (`space-y-3`) e il padding verticale dei singoli pulsanti (`py-2.5`) per occupare lo spazio in modo armonioso.
  * **Rimozione Tasto Indietro Ridondante**: Rimosso il pulsante "Indietro" interno alla pagina di configurazione delle impostazioni. Sincronizzato lo stato tramite prop `$bindable()` per far sì che il pulsante in alto a sinistra della sidebar gestisca in modo intelligente sia il ritorno alla panoramica impostazioni che l'uscita.



